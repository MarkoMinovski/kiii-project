import {createTheme} from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: "#d7d6dc"
        },
        secondary: {
            main: "#901db0"
        },
    }
})


export default theme;
